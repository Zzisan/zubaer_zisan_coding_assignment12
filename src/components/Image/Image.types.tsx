export interface ImageProps {
  src: string;
  alt: string;
  width?: string | number;
  height?: string | number;
  rounded?: boolean;
  bordered?: boolean;
}

export {}; // Ensures this file is treated as a module
